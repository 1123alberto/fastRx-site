{
  "name": "fastrx-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "lint": "node --check app.js && node --check api/contact.js"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
